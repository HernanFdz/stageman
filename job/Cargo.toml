# The doing. May name the domain and nothing else in this workspace —
# `stageman-orchestrator` is absent from the list below on purpose.

[package]
name = "stageman-job"
description = "Workspace isolation, agent supervision and tool hosting for stageman. Internal to the workspace."
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
stageman-core.workspace = true

[lints]
workspace = true
